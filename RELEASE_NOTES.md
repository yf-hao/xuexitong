# 版本说明 (Release Notes) - V0.12.4

## 📅 更新日期: 2026-06-26

### 👨‍🏫 教师分配交互优化 (Teacher Assignment UX)
- **默认不预选教师**: 在班级管理中分配教师时，默认不再自动勾选所有教师，避免误操作导致不必要的分配。
- **全选与全不选按钮**: 分配教师弹窗新增"全选"与"全不选"快捷按钮，支持一键批量勾选或清除，大幅提升大型教学团队的管理效率。

---

# 版本说明 (Release Notes) - V0.12.3

## 📅 更新日期: 2026-06-26

### 📦 Excel 导入与打包机制修复 (Excel Import & Packaging Fix)
- **打包依赖补全**: 修复了应用在打包（PyInstaller）发布后，由于动态导入延迟加载机制导致无法找到 `core.excel_parser` 模块而引发闪退/报错的问题。现在改用模块头部静态导入，并将其显式加入所有平台的 `.spec` 配置文件中，确保打包后的 Excel 导入学生功能 100% 可用。

### 📐 题库公式解析与弹窗优化 (LaTeX & Question Bank Polish)
- **LaTeX 解析增强**: 修复了数学题库上传过程中部分特殊 LaTeX 格式公式的解析 Bug，并补充了完整的自动化单元测试。
- **弹窗样式优化**: 修复了题库上传相关对话框在切换亮/暗色主题时的对比度及样式适配问题，保证界面的视觉统一性。

### 🎨 共享样式与主题回调重构 (Theme Styles & Palette Callbacks)
- **样式系统重构**: 将大部分 UI 组件的静态共享样式重构为动态的调色板回调机制，显著提高了主题系统在各种交互状态下的表现。

---

# 版本说明 (Release Notes) - V0.12.2

## 📅 更新日期: 2026-06-01

### 🤖 AI 拟答兼容性与稳定性优化 (AI Draft Compatibility)
- **优先使用流式响应**: 针对部分 OpenAI 兼容服务非流式返回空内容的问题，优化为优先使用流式接口获取 AI 拟答结果，提升生成成功率。
- **UTF-8 解码修复**: 强制按 UTF-8 处理 AI 返回内容，修复中文回答显示为乱码的问题。
- **接口类型适配**: 完善 `/v1/chat/completions` 与 `/v1/responses` 的接口类型切换逻辑，增强不同 AI 服务网关下的兼容性。

### 🛑 退出流程与窗口稳定性修复 (Shutdown Stability)
- **退出不再二次确认**: 点击退出时直接退出登录，减少不必要的确认弹窗。
- **关闭崩溃修复**: 优化窗口关闭和后台线程清理流程，降低 macOS 下关闭应用时出现 `python quit unexpectedly` 的风险。

### ☁️ 云盘批量操作体验优化 (Cloud Drive UX)
- **批量下载后自动取消勾选**: 在云盘中勾选多个文件或文件夹并下载完成后，列表会自动清除勾选状态，避免重复操作误触发。
- **多文件上传体验延续**: 保留云盘多文件上传能力，减少逐个选择上传的重复操作。

### 🎨 亮色主题与界面可读性修复 (Light Theme Polish)
- **蓝色主按钮白字显示**: 修复亮色模式下蓝底按钮被主题系统错误替换为黑字的问题，登录按钮、主按钮和课程菜单选中项均保持蓝底白字。
- **课程菜单视觉优化**: 左侧“课程菜单”标题改为蓝底白字，提升亮色模式下的层次感和可读性。
- **AI 设置入口简化**: 将顶部“AI 设置”入口改为图标按钮，减少顶部栏拥挤。

---

# 版本说明 (Release Notes) - V0.12.1

## 📅 更新日期: 2026-05-25

### 🚀 自动化部署修复与文件发布优化 (CI/CD Deploy Polish)
- **修复 Release 附件漏发问题**: 修复了由于 GitHub Actions 下载合并 Artifacts 时产生 `dist/` 相对路径差，导致 `softprops/action-gh-release` 的一级非递归匹配通配符漏掉 Windows 及 macOS 产物的问题。通过引入 `dist-artifacts/*` 与 `dist-artifacts/dist/*` 联合扫描规则，保证了全平台打包产物（DMG、Windows Setup.exe、ZIP、DEB 等）均能 100% 完整发布。

### ⚡ 核心日志系统重构与稳定性保障 (Logger System Revamp)
- **全局标准流重定向**: 实现了对 `sys.stdout` 和 `sys.stderr` 的统一劫持与重定向，使全站所有 Python `print` 输出和异常堆栈自动导入标准日志流，杜绝静默失败。
- **发布版日志等级限制**: 在发布版/打包版（`frozen` 状态下）中，默认将文件写入日志等级设为 `logging.ERROR`（开发模式下仍为 `logging.DEBUG`），极大减少了高频 IO 磁盘写入开销，保护机械硬盘并防止打包程序因高频日志产生卡顿。
- **调试日志深度清理**: 清理并移除了分散在各 API 模块（考勤、题库、课程管理等）中的冗余本地调试日志文件（如 `xuexitong_debug.log`, `qbank_submit_debug.log` 等），所有调试信息统一归入标准 `logger.debug` 进行集中管理。
- **启动初期崩溃捕捉**: 重构了 `main.py` 的早期日志初始化逻辑，支持在极早阶段（未完全导入 PyQt 之前）开始日志捕获，帮助精准排查导入库阶段导致的“闪退”故障。

### 📚 作业库分页异步加载 (Homework Pagination & Scaling)
- **大作业量不卡顿**: 重构了作业库（Homework Library）API 数据接口，全面升级为**分页加载（Pagination Loading）**机制，支持在后台高效切片拉取历史作业，彻底解决了拥有几百个作业的大体量课程加载缓慢甚至网络超时的问题。

### 💬 聊天系统 Windows 闪烁修复与性能调优 (IM & Chat Polish)
- **彻底消除 Ghost Window 闪烁**: 修复了 Windows 平台下，在消息列表中切换或加载会话时 QLabel 在未完成父级挂载前被调用 `setVisible(True)` 导致在任务栏或桌面上短暂闪现“空白标题栏小窗口（Ghost HWND）”的问题。
- **智能避免重复轮询**: 优化了 `_refresh_message_tab` 的自动刷新机制，在 msync 状态处于活跃连接时自动跳过冗余的 HTTP 凭证拉取和后台线程刷新轮询，节省网络带宽。

### 📊 学习统计 UI 细节优化与测试覆盖 (UI Polish & Test Coverage)
- **考勤列表精简**: 优化了 `StudyStatusView` 的数据表格展示，移除了使用频率较低且与“活动时间”高度重合的“创建时间”列，并精简了表格列间距，提升在大屏幕和高分屏下的信息呈现清晰度。
- **健壮性自动化测试**: 在 `test_chat_api.py` 中补充了针对标准流重定向、发布版日志分级控制、以及冗余日志文件清理的完整单元测试用例，确保持续集成和后续重构的质量安全。

---

# 版本说明 (Release Notes) - V0.11.0

## 📅 更新日期: 2026-05-20

### ☁️ 全新云盘管理与文件下载 (Cloud Drive & Downloads)
- **批量下载功能**: 新增了 `BatchDownloadThread`，支持在云盘中勾选多个文件或文件夹并进行一键批量下载。
- **下载路径持久化**: 新增自动记忆用户上一次选择的下载路径，避免每次下载都需要重复选择本地保存目录。
- **目录交互与样式重构**: 优化了云盘目录的层级导航和文件列表渲染，亮暗主题下观感更统一、操作更直观。

### ⚡ 极致主题切换性能与渲染热点优化 (Theme Performance Polish)
- **消除切换卡顿**: 针对高频调色板渲染路径（Hotpaths）进行算法级优化，彻底解决了在复杂大班级、多题库界面下亮暗色主题切换时的短暂“冻结”现象。
- **深度代码解耦**: 完成了对管理视图 (`ManagementView`)、考勤视图 (`ActivitiesView`) 和题库视图 (`QuestionBankView`) 等 10 余个千行级复杂 UI 组件的样式解耦，大幅降低运行时内存占用。

### 💬 聊天系统与 IM 凭证获取优化 (IM & Chat Optimization)
- **凭证快速缓存**: 在 `chat_api.py` 中引入了 `get_im_credentials_fast_cached` 缓存快速路径，在主线程上极大避免了频繁网络提取凭证导致的 UI 阻塞及引发的 Ghost Window 现象。
- **测试用例覆盖**: 完善了 IM 模块的单元测试覆盖，确保多平台下消息基础接口的稳定性。

---

# 版本说明 (Release Notes) - V0.10.0

## 📅 更新日期: 2026-05-17

### 🎨 全局亮/暗色双轨主题系统 (Global Theming System)
- **底层架构升级**: 重磅推出自研的动态主题管理引擎 (`ThemeManager`)，深度剥离硬编码的 CSS 样式。
- **全场景覆盖**: 覆盖主窗口、登录界面及多达 20 多个业务对话框与视图（考勤、作业、聊天等）。
- **个性化体验**: 支持“暗色(Dark)”与“亮色(Light)”一键切换，并能持久化记忆用户偏好配置。
- **动态正则渲染**: 独创基于正则表达式的实时样式同步技术，确保在应用运行时动态切换主题毫无闪烁与延迟。

---

# 版本说明 (Release Notes) - V0.9.0

## 📅 更新日期: 2026-05-14

### 🚀 极致启动性能 (Performance Optimization)
- **原生启动优化**: 全平台打包架构从 `onefile` 切换为 `onedir`。彻底解决了 Windows 和 macOS 下由于解压导致的启动延迟，实现**秒级闪电启动**。
- **架构重构**: 优化了安装程序与系统路径的交互逻辑，Windows 安装版现在会自动管理依赖目录，运行更加稳定。

### ✍️ 全功能作业发布系统 (Homework Publishing)
- **多维发布弹窗**: 新增完整的作业发布设置界面，支持设置开始/截止时间、自动提醒、及格线及重做次数。
- **防作弊与规则定制**: 集成题目随机排序、选项随机排序及禁止粘贴功能。

### ⚡ 考勤修改交互革命 (Interaction Polish)
- **N 键连击模式**: 状态修改弹窗新增“下一个”按钮及 **N 键快捷键**。支持一键提交并自动跳转至下一位学生，极大提升大班点名效率。
- **UI 细节优化**: 表格新增隔行变色支持，优化了已签/未签列表在修改状态后的即时同步逻辑。

### 🏷️ 发布命名标准化 (Distribution Standards)
- **版本化命名**: 所有平台的安装包文件名现在统一包含版本号（如 `XHelper-0.9.0-Setup.exe`）。
- **Linux 规范化**: `.deb` 安装包现在遵循标准，将程序安装至 `/opt/xhelper` 目录。

---

# 版本说明 (Release Notes) - V0.8.1

## 📅 更新日期: 2026-05-13

### 🧐 考勤细节与视觉增强 (Attendance UX Boost)
- **大屏学生信息展示**: 在修改签到状态时，新增学生姓名与学号的超大字号展示，确保老师在任何距离都能准确识别。
- **自定义缩放支持**: 引入动态缩放机制，支持通过界面按钮或快捷键实时调整字体大小，满足不同视力或屏幕分辨率需求。
- **配置持久化记忆**: 自动保存你偏好的字体大小设置，跨会话保持最佳观看体验。
- **界面布局重构**: 优化了状态编辑对话框的布局，使信息层级更加清晰。

---

# 版本说明 (Release Notes) - V0.8.0

## 📅 更新日期: 2026-05-13

### 🔍 考勤筛选与体验优化 (Attendance Search & UX)
- **快捷搜索功能**: 签到详情界面新增搜索栏，支持通过“姓名”或“学号”快速定位学生。特别适合处理大班课（百人以上）的现场考勤。
- **实时过滤交互**: 输入文字实时筛选，清空搜索框自动恢复全量列表，大幅提升查找效率。
- **UI 布局优化**: 搜索框巧妙集成于标签栏右上角，保持界面整洁的同时提供即时访问。

### 🧪 稳定性增强 (Stability)
- **单元测试全覆盖**: 针对搜索逻辑、列表刷新机制新增了多项单元测试，确保在大数据量下的稳定性。
- **状态同步优化**: 进一步优化了修改状态后的 UI 刷新逻辑，减少界面闪烁。

---

# 版本说明 (Release Notes) - V0.7.0

## 📅 更新日期: 2026-05-12

### ✍️ 考勤管理深度增强 (Attendance Management)
- **手动修改签到状态**: 现在可以直接在应用中修改学生的签到状态（如将“缺勤”改为“正常”或“事假”），支持实时同步至学习通后台。
- **新增“公假”类型**: 正式支持“公假”（状态码 12）的显示与统计，满足学校活动请假场景。
- **智能化分组显示**: 签到详情界面重构为“已签”和“未签”两大直观分组，修改状态后学生会自动在分组间平滑移动。
- **快捷备注支持**: 在修改状态时支持添加备注信息，方便后续查证。

### 🐞 细节修复与优化 (Bug Fixes & Refactor)
- **数据模型重构**: 优化了 `AttendanceRecord` 模型，提高了状态更新和统计计算的效率。
- **依赖补全**: 修复了上个版本中缺失 `websocket-client` 导致部分功能闪退的问题。

---

# 版本说明 (Release Notes) - V0.6.0

## 📅 更新日期: 2026-05-11

### 📦 全平台安装程序支持 (Distribution Upgrade)
- **多平台安装包**: 不再仅提供免安装压缩包！现在为不同系统提供原生安装程序：
  - **Windows**: `.exe` 安装包 (支持开始菜单和桌面快捷方式)。
  - **macOS**: `.dmg` 磁盘镜像 (支持拖拽安装)。
  - **Linux**: `.deb` 软件包 (支持 Ubuntu/Debian 系列一键安装)。
- **自动化发布**: 所有安装包均由 GitHub Actions 自动构建，确保运行环境纯净。

### 💬 消息发送功能初探 (Messaging Preview)
- **底层架构搭建**: 引入即时通讯模块，完成 WebSocket 通信及加密协议的基础对接。
- **消息发送流程**: 初步实现消息发送功能，支持教学过程中的主动信息干预。
- **批量提醒工作流**: 结合消息发送能力，新增批量提醒工作流，提升教学任务催促效率。

### ⚡️ 性能与体验优化 (Performance & UX)
- **毫秒级启动**: 深度优化启动流程，大幅缩短软件从点击到进入主界面的等待时间。
- **交互修复**: 修复了会话处理中的多处逻辑漏洞，确保二维码登录及会话保持更加稳定。
- **渲染微调**: 持续改进题库管理中的表格显示效果及渲染逻辑。

---

# 版本说明 (Release Notes) - V0.5.8

## 📅 更新日期: 2026-05-07

### ⚡️ 签到活动交互增强 (Activity UX)
- **快捷控制按钮**: 在活动列表中直接集成了“开始”、“结束”和“删除”按钮，大幅提升课堂教学时的操作效率。
- **二维码弹窗**: 支持在活动详情中一键弹出签到二维码。
- **新活动自动提醒**: 增加新活动自动弹出机制，确保第一时间捕获课程新发布的活动。

### 📚 题库管理效率提升 (Question Bank UX)
- **批量勾选功能**: 题库列表新增表头全选及单项勾选，支持对题目进行批量管理（如批量删除）。
- **渲染排版优化**: 修复了 KaTeX 数学公式在部分复杂布局下的换行溢出问题，提升阅读体验。

---

# 版本说明 (Release Notes) - V0.5.7

## 📅 更新日期: 2026-04-29

### 🚀 渲染性能飞跃 (Hot Rendering)
- **毫秒级极速响应**: 引入了“热渲染”机制，通过在内存中常驻渲染引擎并动态更新 DOM，彻底消除了每次渲染公式都要重载页面的开销。渲染速度提升超过 10 倍，即便在打包后的程序中也能实现瞬时响应。
- **单例引擎优化**: 重构为单例模式，复用 Chromium 渲染实例，大幅降低了批量上传题目时的系统资源占用和初始化等待时间。

### 📤 上传交互优化 (Upload UX)
- **实时进度对话框**: 新增了上传进度条，实时显示当前上传题目的序号和总数。
- **任务可控性**: 在进度对话框中增加了“取消”按钮，支持用户随时中止上传任务。
- **视觉回归**: 根据用户反馈，将公式图片的背景色优化回纯白色 (#ffffff)，确保在学习通等平台的各种背景下均有最佳的显示对比度和清晰度。

---

# 版本说明 (Release Notes) - V0.5.6

## 📅 更新日期: 2026-04-29

### ✨ LaTeX 公式渲染引擎重构 (KaTeX Rendering)
- **本地化高质量渲染**: 引入了基于无头浏览器 (Headless WebEngine) 的本地 KaTeX 快照渲染机制。现在处理复杂数学公式（如矩阵、求和、积分等）时，将优先使用本地 KaTeX 引擎，不仅提升了渲染速度，还大幅提高了图片排版质量，有效解决网络受限时的公式丢失问题。
- **解析算法增强**: 深度扩展了 LaTeX 符号映射库，完美支持变量被 `[]` 包裹时的特殊上下标结构转换（如数组下标）。
- **图片排版自适应**: 优化了生成的公式图片的 HTML 标签属性，引入 `max-width: 100%` 和自适应宽度，确保超长公式在各端显示时不会破坏版面。

### 🛠️ 稳定性与构建升级
- **CI/CD 自动化冒烟测试**: 在 GitHub Actions 流水线中引入了环境依赖包（如 `xvfb`）以支持 Linux 端的无头化运行，并在正式打包前加入 **KaTeX 冒烟测试 (Smoke Test)**，彻底杜绝了因环境缺失导致公式引擎静默失效的风险。
- **体验修复**: 修复了题库目录树在根节点 (`root`) 刷新时逻辑不生效的 Bug。

---

# 版本说明 (Release Notes) - V0.5.5

## 📅 更新日期: 2026-04-27

### ✨ 位置管理系统增强 (Location Management)
- **导入与导出**: 支持自定义位置的 `.txt` 格式导入与导出，方便用户备份或在不同设备间迁移常用位置数据。
- **内置位置管理**: 允许隐藏系统内置的常用位置（如西亚斯各教学楼），使列表更加清爽，专注于个人常用地点。
- **交互逻辑优化**:
    - 优化了位置配置对话框的 Tab 切换逻辑，切换时自动重置输入状态，防止误操作。
    - 改进了位置列表的选中反馈，默认不再自动选中第一项，减少数据干扰。
    - 修复了删除位置时的二次确认逻辑，增加了对内置位置的特殊保护提示。

### 🛠️ 细节改进与修复
- **范围值对齐**: 统一并微调了默认签到范围（Range）的初始建议值，提升签到成功率。
- **UI 稳定性**: 增强了数据加载时的信号屏蔽机制，避免在初始化界面时触发不必要的逻辑。

---

# 版本说明 (Release Notes) - V0.5.4

## 📅 更新日期: 2026-04-16

### ✨ 核心功能升级 (Core Features)
- **地理位置签到**: 为签到任务增加了“开启位置”开关，完美支持需要地理位置校验的课程签到。
- **UI 交互细节优化**: 
    - 题库视图新增操作指引，解决首次进入时的空白迷惑感。
    - 优化了全站部分界面组件的布局和文本提示，操作更直观。

---

# 版本说明 (Release Notes) - V0.5.3

## 📅 更新日期: 2026-04-15

### ✨ 新增特性与优化 (New Features & Improvements)
- **数据导出增强**: 新增对缺勤统计、作业次数的Excel 导出功能。
- **人员信息管理**: 实现了缺勤统计中的学生班级信息显示。
- **题库维护**: 题库系统增加了题目删除功能，补全了全生命周期的数据管理闭环。

---

# 版本说明 (Release Notes) - V0.5.2

## 📅 更新日期: 2026-04-13

### 🛠️ 全平台启动性能与稳定性优化
- **权限与路径深度修复**: 彻底解决了 `CommunicationManager` 等组件在只读文件系统下（如 `/Applications`）尝试创建 `data` 目录导致的崩溃。所有持久化数据现已迁移至系统级用户应用支持目录。
- **异常诊断系统上线**: 实现了全阶段异常捕获日志（`app.log`）及 GUI 错误对话框。即使在导入库阶段发生崩溃，也能提供完整的堆栈信息。
- **窗口切换稳定性优化**: 引入了 `setQuitOnLastWindowClosed(False)` 机制，解决了 macOS 在登录窗口到主窗口过渡期间因瞬时无可见窗口而导致程序意外退出的问题。
- **资源加载路径纠偏**: 统一了 KaTeX 等静态资源在 PyInstaller 打包模式下的定位逻辑。

---

# 版本说明 (Release Notes) - V0.5.1

## 📅 更新日期: 2026-04-11

### 🛠️ 紧急修复与部署优化
- **依赖库深度对齐**: 修复了 macOS 打包程序缺失 `PyQt6-WebEngine` 导致无法启动的问题。
- **CI/CD 流程优化**: 强化了 GitHub Actions 自动部署流程，确保云端构建环境与本地环境依赖完全同步。

---

# 版本说明 (Release Notes) - V0.5.0

## 📅 更新日期: 2026-04-11

这是《学习通教学辅助系统》具有里程碑意义的 **V0.5.0** 版本！🚀 标志着系统从基础辅助工具正式进化为全方位的教学资源管理平台。

## ✨ 重大更新与新增特性 (New & Core Features)

### 1. ☁️ 云盘管理系统 (Cloud Drive)
- **API 深度集成**: 实现了云盘功能的完整协议对接，支持查看和管理个人/课程云端资源（部分功能持续完善中）。

### 2. 📚 题库系统 2.0 (Question Bank Evolution)
- **分页与矩阵支持**: 题库列表支持高效的分页查询，并新增了对**矩阵 (Matrix)** 等复杂数学结构的渲染支持。
- **增强型 Markdown**: 支持 Markdown 代码块、粗体等多种格式转换，满足计算机及理工科题目的排版需求。

### 3. 📝 作业闭环管理 (Homework Management)
- **作业库视图**: 新增 `Homework Library` 视图，方便快速检索和复用历史作业任务。
- **在线创建流程**: 深度集成 `Homework Create` 功能，支持从题库直接选取内容生成新的作业。

### 4. 📐 科学符号与公式支持 (KaTeX)
- **全站 KaTeX 集成**: 题目预览、内容展示均已支持 **KaTeX 数学公式渲染**，提供印刷级的公式显示效果。

### 5. 🛠️ 构建与稳定性优化
- **全平台构建脚本更新**: 优化了 Windows, macOS, Linux 的 `.spec` 打包配置。
- **依赖库规范化**: 同步更新 `requirements.txt`，确保环境部署的一致性。

---

# 版本说明 (Release Notes) - V0.3.0

## 📅 更新日期: 2026-04-06

这是《学习通教学资源管理系统》的 **V0.3.0** 里程碑版本！🎉 本次更新引入了核心“活动”管理模块及全异步架构升级。

## ✨ 重大更新与新增特性 (New & Core Features)

### 1. 🚀 核心“活动管理”系统上线
- **批量签到计划**: 支持按照周次、单双周规律一键生成全学期签到预设。
- **逆序一键发布**: 完美解决学习通“先发后显”逻辑，支持将本地预设的签到任务逆序批量推送到服务器。
- **活动分组管理**: 实现对课程分组的同步、重命名、新增及彻底删除，完美适配教师端管理场景。

### 2. 🏗️ 全异步 Worker 架构重构
- **深度多线程化**: 所有网络请求及耗时操作（发布、同步、删除）均已迁移至后台 Worker 线程。
- **界面零掉帧**: 无论是在进行批量发布还是数据同步，UI 界面依然保持丝滑顺畅。
- **稳健的 API 层**: 统一了数据模型与网络层接口，大幅降低了接口访问失败的概率。

### 3. 🎨 UI/UX 与个性化增强 (HaoMD)
- **HaoMD 预览优化**: 优化了 KaTeX 公式渲染性能，支持更复杂的数学公式展示。
- **配置持久化**: 增强了“配置记忆”功能，自动记录并恢复上次选择的班级、分组及签过设置。
- **批量发布进度**: 新增实时任务队列监控，发布进度一目了然。

### 4. 🛠️ 稳定性与维护性
- **路径自平衡**: 优化了 `SIGNIN_DATA_FILE` 等本地存储路径，确保跨平台部署时的文件隔离。
- **错误捕获**: 完善了 API 返回值的深度校验。

---

# 版本说明 (Release Notes) - V0.2.1

## 📅 更新日期: 2026-03-24

这是《学习通教学资源管理系统》首次公开开源发布的 `V0.2.1` 正式版本！🎉

## ✨ 重大更新与新增特性 (New & Core Features)

### 1. 首次开源开源发布
- 彻底清理了历史构建和测试数据，开源发布初始版本。
- 确保所有的 `.spec` 打包配置以及 `.gitignore` 规范可用，支持所有开发者一键构建。

### 2. 多平台全自动打包构建体系 (CI/CD workflows)
- **GitHub Actions 集成**: 实现了向打标签/推送自动触发发版的完整工作流。
- **真正的全平台支持**: 借助最新的 GitHub Actions 实现了 **macOS (Intel & ARM64)**、**Windows**、**Linux (Ubuntu)** 三大系统的自动无缝并行打包。
- **依赖深度修复**: 彻底解决了 `PyInstaller` 无法隐式打包 `Crypto` 等三方模块的问题，确保所有系统编译出的独立程序完美运行。

### 3. 核心管理功能上线
- 完善了班级管理机制（导入、成绩导出、自动化统计）。
- 增加了安全与稳定性的本地会话维持手段。

---


# 版本说明 (Release Notes) - V0.1

## 📅 更新日期: 2026-01-24

## ✨ 新增功能 (New Features)

### 1. 班级管理系统 (Class Management)
- **批量导入学生**: 支持解析树维教务系统导出的 Excel 成绩登记册，将学生批量添加至指定班级。
- **班级维护**: 新增重命名班级、归档（删除）班级功能。
- **教师分配**: 支持将班级分配给教学团队中的特定教师。
- **新建班级**: 提供了新建班级的入口（API已集成）。

### 2. 成绩权重管理 (Grade Weights)
- **可视化配置**: 提供了直观的百分比滑块/输入框，用于调整各项成绩权重（如视频、测验、作业、考试等）。
- **一键同步**: 支持将当前权重配置一键同步应用至课程下的所有班级。
- **实时校验**: 自动计算权重总和，确保总和为 100% 时才允许保存，防止配置错误。

### 3. 统计数据导出与管理 (Statistics)
- **多类型支持**: 除了考勤数据外，新增了 **测验**、**作业**、**综合成绩** 的导出支持。
- **历史记录管理**: 支持在下载中心直接删除已生成的历史导出记录。
- **下载功能**: 修复了下载链接的获取逻辑，支持直接调用默认浏览器下载导出文件。

### 4. 教师团队管理 (Teacher Team)
- **界面集成**: 新增教师团队管理入口，支持查看和管理课程团队成员（部分功能开发中）。

---

## 🛠 优化与改进 (Improvements)

- **UI/UX 升级**: 
  - 登录界面适配暗黑模式 (Dark Theme)。
  - 管理界面采用卡片式布局，操作更直观。
  - 优化了下拉菜单和按钮的交互动效。
- **打包部署**:
  - 优化了 macOS 打包脚本 (`build.sh` + `.spec`)，生成单一可执行文件。
  - **路径修复**: 修复了打包后 `data` 目录路径识别错误的问题，现在支持在可执行文件同级目录下读取配置文件。
- **性能优化**: 
  - 采用多线程 (Worker Threads) 处理耗时网络请求，避免界面卡顿。
  - 增加了加载状态提示，提升用户等待体验。

## 🐛 问题修复 (Bug Fixes)

- 修复了“导入课程”界面闪退的问题。
- 修复了删除统计报表时 ID 提取失败的问题。
- 修复了打包后无法读取 `signin_plans.json` 等配置文件的路径错误。
- 修正了部分 API 请求头缺失导致学习通返回 403/404 的问题。
